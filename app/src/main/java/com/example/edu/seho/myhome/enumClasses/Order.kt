package com.example.edu.seho.myhome.enumClasses

/** @author Sebastian Holm
 *  Enum class used when selecting the ordering of the items in the storage Database
 */
enum class Order {
    Id,
    Category,
    Date,
    Name
}